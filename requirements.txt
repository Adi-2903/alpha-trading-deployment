# -- Alpaca connectivity --
requests>=2.31

# -- pricing_engine (vendored from Jenil's options_pricing_engine) --
numpy>=1.26
scipy>=1.11

# -- research layer: regime detection + vol forecasting --
pandas>=2.0
hmmlearn>=0.3
arch>=6.3
scikit-learn>=1.3

# -- backtest (offline, no Alpaca calls) --
yfinance>=0.2
matplotlib>=3.8

# -- API backend for the UI --
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# -- tests --
pytest>=8.0
